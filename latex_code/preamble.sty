
% Name of the compendium pdf file
\def\jobname{TMA4267}

\usepackage{amsmath, amssymb, amsthm, amsfonts}
\usepackage{graphicx}
\usepackage{parskip}
\usepackage[english]{babel}   % Språk
\usepackage[utf8]{inputenc}
\usepackage{csquotes}
\usepackage{float}
\usepackage{caption}
\captionsetup{font=small}
%\numberwithin{equation}{section} % Likning 3.1 etc

% Figures made in LaTeX, in particular: equations with explanatory arrows. 

\usepackage{pgfplots}
\pgfplotsset{compat=1.17}

\usepackage{tikz} 
\usetikzlibrary{positioning}
\usetikzlibrary{calc}
\usepackage{xcolor}
\NewDocumentCommand{\EqM}{ m O{black} m}{\tikz[remember picture, baseline, anchor=base] \node[inner sep=0pt, outer sep=3pt, text=#2] (#1) {\ensuremath{#3}};}

% Notes in margin
\usepackage{marginnote} 

%%% Todo liste greier
\usepackage{multicol} % flere kolonner i del av dokument
\usepackage{enumitem}   % Variere lister
\newlist{todolist}{itemize}{2}
\setlist[todolist]{label=$\square$}
\usepackage{pifont}
\newcommand{\cmark}{\ding{51}}%
\newcommand{\xmark}{\ding{55}}%
\newcommand{\done}{\rlap{$\square$}{\raisebox{2pt}{\large\hspace{1pt}\cmark}}%
\hspace{-2.5pt}}
\newcommand{\wontfix}{\rlap{$\square$}{\large\hspace{1pt}\xmark}}

%%% Kilde greier
\usepackage[backend=biber,
    sortcites  = true,
    giveninits = true,
    isbn = false,
    url = true,
    maxcitenames=1,
    citestyle=numeric,
    style=numeric]{biblatex} 
\DeclareNameAlias{sortname}{family-given} 
\DeclareNameAlias{default}{family-given} 
\addbibresource{Compendium/sources.bib}

% Complete ref (no need to write figure...)
% NB clever after hyper
\usepackage{hyperref}
\usepackage{xcolor}           % set colors
\hypersetup{
    colorlinks = false,
    linkbordercolor = {white}}
\usepackage{cleveref}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%% EGENDEFINERTE KOMMANDOER %%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% Italics (not in use)
% \newcommand{\bt}[1]{\textbf{#1}\mbox{}}
% \newcommand{\tit}[1]{\textit{#1}\mbox{}}
% \newcommand{\btit}[1]{\textbf{\textit{#1}}\mbox{}}

% Definitions that go in index
\newcommand{\term}[1]{\emph{#1}\index{#1}}

% Colored text for TODO etc
\newcommand{\red}[1]{\color{red}{#1}\normalcolor} 
\newcommand{\TODO}[1]{\color{red}{TODO: #1}\normalcolor} 

% Shorthand for common commands
\newcommand{\bs}[1]{\boldsymbol{#1}}
\newcommand{\mr}[1]{\mathrm{#1}}

% Symbols typical for statistics
\newcommand{\var}[1]{\mathrm{Var}\left[#1\right]} % variance
\newcommand{\cov}[1]{\mathrm{Cov}\left[#1\right]} % variance
\newcommand{\ev}[1]{\mathbb{E}\left[#1\right]}
\newcommand{\p}[1]{\mathbb{P}\left[#1\right]}
\renewcommand{\hat}[1]{\widehat{#1}} % estimator hat

\newcommand{\col}[1]{\mathrm{col}\left(#1\right)} % column space
\newcommand{\rank}[1]{\mathrm{rank}\left(#1\right)} % rank
\newcommand{\tr}[1]{\mathrm{tr}\left(#1\right)} % trace

\newcommand{\bh}{\widehat{\beta}}
\renewcommand{\b}{\beta}
\newcommand{\sh}{\widehat{\sigma}}
\newcommand{\s}{\sigma}
\newcommand{\eh}{\widehat{\varepsilon}}
\newcommand{\e}{\varepsilon}
\newcommand{\mh}{\widehat{\mu}}

\newcommand{\Bh}{\widehat{\boldsymbol{\beta}}}
\newcommand{\B}{\boldsymbol{\beta}}
\newcommand{\Sh}{\widehat{\boldsymbol{\sigma}}}
\renewcommand{\S}{\boldsymbol{\sigma}}
\renewcommand{\SS}{\boldsymbol{\Sigma}}
\newcommand{\Eh}{\widehat{\boldsymbol{\varepsilon}}}
\newcommand{\E}{\boldsymbol{\varepsilon}}
\newcommand{\M}{\boldsymbol{\mu}}
\newcommand{\Mh}{\widehat{\boldsymbol{\mu}}}

\newcommand{\x}{\boldsymbol{x}} % vectors
\newcommand{\y}{\boldsymbol{y}}
\newcommand{\z}{\boldsymbol{z}}
\newcommand{\Z}{\boldsymbol{Z}}
\newcommand{\X}{\boldsymbol{X}}
\newcommand{\Y}{\boldsymbol{Y}}
\newcommand{\mA}{\boldsymbol{A}} % matrices
\newcommand{\mB}{\boldsymbol{B}}
\newcommand{\mC}{\boldsymbol{C}}
\newcommand{\mH}{\boldsymbol{H}}
\newcommand{\mG}{\boldsymbol{\Gamma}}
\newcommand{\mL}{\boldsymbol{\Lambda}}
\newcommand{\mI}{\boldsymbol{I}}
\newcommand{\zero}{\boldsymbol{0}}
\newcommand{\one}{\boldsymbol{1}}

%% Double letters
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\N}{\mathbb{N}}

% Argmin and argmax
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}

% Never write an ugly phi again
\renewcommand{\phi}{\varphi}
\renewcommand{\epsilon}{\varepsilon}

% Shorthands
\newcommand{\qall}{\quad\forall}

% Normer og parenteser
\newcommand{\norm}[1]{\lVert #1 \rVert}
\newcommand{\abs}[1]{\lvert #1 \rvert}
\newcommand{\brac}[1]{\left( #1 \right)}
\newcommand{\bracc}[1]{\left\{ #1 \right\}}
\newcommand{\brak}[1]{\left[ #1 \right]}
\newcommand{\bracp}[1]{\left\langle #1 \right\rangle}

%%%%%%%%%%%% EGENDEFINERTE environments %%%%%%%%%%%%

\newtheorem{proposition}{Proposition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newtheorem*{remark}{Remark}

\theoremstyle{definition}
\newtheorem{definition}{Definition}
